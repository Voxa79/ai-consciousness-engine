# Exemple de configuration d'environnement pour l'app web (Create React App)
# Copiez ce fichier sous le nom ".env" dans ce même dossier et renseignez les valeurs.
# ATTENTION: Ne commitez jamais vos secrets. Laissez uniquement ce fichier d'exemple.

# URL de l'API backend (local dev)
REACT_APP_API_BASE_URL=http://localhost:3000

# URL WebSocket (gateway → Ollama) en local
REACT_APP_WS_URL=ws://localhost:3001/ws

# Environnement de l'app (development|staging|production)
REACT_APP_ENV=development

# Outils d'observabilité (optionnel)
REACT_APP_SENTRY_DSN=

# Analytics (optionnel)
# Provider disponible: grafana (embedding)
REACT_APP_ANALYTICS_PROVIDER=grafana
# URL Grafana (local via docker-compose)
REACT_APP_GRAFANA_URL=http://localhost:3002
# Identifiants d'embed (optionnels selon intégration UI)
REACT_APP_GRAFANA_DASHBOARD_UID=
REACT_APP_GRAFANA_PANEL_ID=
REACT_APP_GRAFANA_EMBED=true

# Feature flags (JSON recommandé)
REACT_APP_FEATURE_FLAGS={}

# Observabilité / Monitoring (liens UI optionnels)
# Prometheus (scraping UI)
REACT_APP_PROMETHEUS_URL=http://localhost:9090
# Elasticsearch (console)
REACT_APP_ELASTICSEARCH_URL=http://localhost:9200
# Kibana (UI Elasticsearch)
REACT_APP_KIBANA_URL=http://localhost:5601

# Intégration modèles locaux (Ollama) — pour tests en conteneur Docker
# URL du serveur Ollama (par défaut: http://localhost:11434)
REACT_APP_OLLAMA_BASE_URL=http://localhost:11434
# Modèle à utiliser (ex: qwen2.5:3b-instruct-q4_k_m)
REACT_APP_OLLAMA_MODEL=qwen2.5:3b-instruct-q4_k_m

# Paramètres de dev local (non utilisés en prod Netlify)
# Port du dev-server CRA: npm start
PORT=3000
# Pour forcer l'ouverture navigateur: (laissez vide pour comportement par défaut)
BROWSER=