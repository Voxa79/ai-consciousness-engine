# web-ui/.env.production

# Indique au front qu'on est en prod
REACT_APP_ENV=production

# Endpoint HTTP public de l'API/proxy qui route vers tes containers Docker (Ollama)
# Remplacer par ton domaine public (Cloudflare Tunnel/Ngrok ou micro-proxy)
REACT_APP_API_BASE_URL=https://ce-api.example.com

# Endpoint WebSocket public pour le temps réel
REACT_APP_WS_URL=wss://ce-api.example.com/ws

# Modèle Ollama par défaut
REACT_APP_DEFAULT_MODEL=qwen2.5:3b-instruct-q4_k_m

# URL publique Grafana (dashboard embarqué)
REACT_APP_GRAFANA_URL=https://grafana.example.com/d/ce/overview?orgId=1&kiosk

# Activer l'embed analytics
REACT_APP_ANALYTICS_ENABLED=true
